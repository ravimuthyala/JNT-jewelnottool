import 'package:flutter/material.dart';

class AppColors {
  // Kept for backward compatibility in older screens; mapped to the current brand palette.
  static const Color deepPlum = Color(0xFF292222);
  static const Color alabaster = Color(0xFFF4EFE1);
  static const Color snow = Color(0xFFFAF9F9);
  static const Color balletSlippers = Color(0xFFEDD9C9);
  static const Color blackCat = Color(0xFF292222);
  static const Color blackCatLight = Color(0xFF6E6262);
  static const Color blackCatBorderLight = Color(0xFFC6BDBD);
  static const Color white = Color(0xFFFFFFFF);
}
