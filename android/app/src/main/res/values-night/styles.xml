<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The app forces ThemeMode.light in Flutter (see main.dart), so the
         native launch treatment must not vary with the OS's Dark Mode
         setting either. This mirrors values/styles.xml exactly instead of
         using a separate black/no-preview-suppression theme, which used to
         cause a different (untuned, briefly white/light) flash only when
         the device was in system Dark Mode. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowDisablePreview">true</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">#292222</item>
    </style>
</resources>
